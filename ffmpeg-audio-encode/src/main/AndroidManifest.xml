<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.onzhou.audio.encode" >

    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.CAMERA"/>

    <application
        android:name="com.onzhou.ffmpeg.base.BaseApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:testOnly="false">
        <activity android:name="com.onzhou.audio.main.AudioEncodeActivity"/>

        <activity android:name="com.onzhou.audio.main.PlayerActivity"/>

        <activity android:name="com.onzhou.ui.RecorderActivity"/>

        <activity android:name="com.onzhou.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
